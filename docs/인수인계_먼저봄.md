# 인수인계 — 먼저봄 파일럿

CLAUDE.md는 전환 이전 상태다. 현행 기준은 작업지시서 v2와 이 문서다.

작성일: 2026-09-24
저장소: `C:\Projects\noma-welfare`
관련 문서: `docs/작업지시서_파일럿_재작업_02_04_05.md` (v2)

---

## 1. 현재 상태

### 브랜치와 커밋

- 브랜치: `pilot-sancheong` (원격과 동기화됨)
- 최신 커밋: `b666eb0` — fix: 1층에 로그아웃 추가, 연계 섹션 문구 정리
- `master`는 건드리지 않았다. 전환 작업은 전부 이 브랜치에 있다.

작업 0 이후 커밋 12건:

| 커밋 | 내용 |
|---|---|
| `b666eb0` | 1층 로그아웃 추가, 연계 섹션 문구 정리 |
| `f94e3b0` | 1층 화면 이름을 먼저봄으로, 마이크 불가 시 직접 입력 전환 |
| `e884103` | **작업 3** — 생활지원사 음성 입력 화면 |
| `cd23208` | 외부 연계 화면 — 연락처 안내로 전환, "요청" 문구 제거 |
| `f016592` | 수락·반려를 받는 기관으로 서버에서 제한 |
| `eca00f8` | 연계 화면 정리 — 외부 연계를 주된 경로로 |
| `017b6ca` | **작업 5** — 외부 연계 결과 기록 |
| `f80a260` | **작업 4** — 연계처 데이터 구조 (가상 5건, 시군 필드) |
| `8a0f013` | **작업 2** — 승인 단계 제거 (요청 → 수락/반려 2단계) |
| `b6b0e6c` | 케이스 목록 응답 형태 불일치 수정 |
| `6b306ca` | **작업 1** — 조직 데이터 전환 (4개 부서 → 산청 수행기관 1곳) |
| `c990def` | **작업 0** — 실행 전제 조치 |

### 작업 진행

작업 0~5 완료. **작업 6(후보 연계처 자동 추천)은 이번 범위에서 제외했다.**

| 작업 | 내용 | 상태 |
|---|---|---|
| 0 | 실행 전제 조치 (공유 비밀번호, 수신자 환경변수) | 완료 |
| 1 | 조직 데이터 전환 (산청 수행기관 1곳) | 완료 |
| 2 | 승인 단계 제거 — `pending`/`accepted`/`rejected` 3종 | 완료 |
| 3 | 생활지원사 음성 입력 화면 (먼저봄) | 완료 |
| 4 | 연계처 데이터 구조 (가상 5건, 시군 필드) | 완료 |
| 5 | 외부 연계 결과 기록 | 완료 |
| 6 | 후보 연계처 자동 추천 | **제외** — 코드 없음 |

### 바뀐 구조 요약

- **승인자가 없다.** 기관 간 이동은 의뢰이며 요청 → 수락 또는 반려 2단계로 끝난다. 구 승인 API(`/api/dept/*`, `/api/target-dept/*`)는 410을 반환한다.
- **수락·반려는 받는 기관만** 한다. 서버에서 `session.deptId === linkage.toDept`를 검증하며, 관리자 세션에는 `deptId`가 없으므로 관리자도 403이다.
- **외부 연계(referral)는 생성 즉시 `accepted`**로 저장된다. 이메일·자식 케이스·원 케이스 상태 변경이 없다. 접수는 시스템 밖에서 이뤄지므로 결과를 `result`에 기록하며, 기록이 곧 완료다.
- **외부 기관은 시스템 사용자가 아니다.** 시스템은 연계처를 안내하고 결과를 기록할 뿐 연락은 전담사회복지사가 직접 한다.
- **1층(먼저봄)과 2층이 분리되어 있다.** worker 세션은 `/api/report/*` 밖으로 나가지 못하며, 1층에는 사업명·신청방법·문의처·추천을 표시하지 않는다.

### 화면과 계정

| 화면 | 경로 | 로그인 |
|---|---|---|
| 1층 — 먼저봄 | `/report` | 이름 직접 입력 + `WORKER_PASSWORD` |
| 2층 — 케이스 | `/case` | 담당자 → 기관 → 서비스 + `ADMIN_PASSWORD` |
| 관리자 | `/admin` | 관리자 + `ADMIN_PASSWORD` |

기관은 `산청 수행기관`(`sancheong-1`)과 `가상 수행기관 B`(`virtual-b`) 두 곳이다. 내부 연계 수락·반려를 확인하려면 받는 기관으로 로그인해야 한다.

### 실기기 확인 결과 (2026-09-24)

| 항목 | 기종 | 브라우저 | 결과 |
|---|---|---|---|
| 음성 입력 | 갤럭시 노트20 | Chrome | 정상 |
| 권한 거부 → 직접 입력 전환 | — | — | 실기기에서 확인 못 함 |
| tel: 링크 | 갤럭시 노트20 | 네이버 브라우저 | 정상 |
| 2층 보고 읽기 | 갤럭시 노트20 | 네이버 브라우저 | 정상 |
| 2층 결과 기록 | 갤럭시 노트20 | 네이버 브라우저 | 정상 |

PC 크롬 확인(로그아웃 / 권한 거부 전환 / 연계 문구)은 **결과 미기입 상태다.** 값을 받으면 이 표에 채운다.

확인 방식: `cloudflared`로 로컬 서버에 임시 HTTPS 터널을 열어 진행했고, 확인 후 터널을 닫았다. `tel:` 확인용으로 `data/linkage_targets_sancheong.json`의 `lt-002`에 가상 번호 `010-0000-0000`을 잠시 넣었다가 **원복했다.** 현재 연계처 5건은 전부 연락처가 빈 값이다.

### 알려진 한계

- **workerName을 로그인 시 직접 입력한다.** 다른 사람 이름으로 들어가면 그 담당 케이스가 보인다. 개인 계정이 없는 구조의 한계다.
- **크롬의 음성 인식은 녹음된 음성을 구글 서버로 전송해** 처리한다. 대상자 상황이 담긴 발화가 외부로 나가므로 실데이터 투입 전 검토가 필요하다.
- 공유 비밀번호 체계라 수락·반려한 사람과 요청한 사람이 구분되지 않는다. 기록되는 `by`는 기관명이다.
- 세션이 MemoryStore라 재배포하면 전원 로그아웃된다.
- `data/requests.json`이 git 추적 대상이다. 실데이터 투입 전 해결해야 한다.

### 범위 밖 발견 사항 (손대지 않음)

- `CLAUDE.md:13`, `CLAUDE.md:122`, `docs/CLAUDE.md:8`의 모델 기재가 코드와 다르다. 문서는 "Gemini 2.0 Flash", 코드는 `gemini-2.5-flash`(`server.js:2219, 2430, 2493, 3211`)다.
- `admin.html`에 서버에 없는 라우트를 호출하는 코드가 남아 있다(`/api/admin/linkage/:id/approve` 등). 화면이 숨겨져 있어 실행되지 않는다.
- `POST /api/referral/:id/send`는 호출하는 화면이 없다. 라우트만 남아 있다.
- `git mv`가 이 환경의 권한 정책에 막혀, 시연 데이터 분리는 복사 후 원본 비우기로 처리했다. git 이력에 rename이 아니라 신규+수정으로 기록되어 있다.

---

## 2. 다음 할 일

**없다.**

9월 30일부터 코드 작업을 하지 않는다. 이후는 응모서 작성이다.
응모서용 화면 캡처가 필요하면 그때 지시한다.

---

## 3. 지켜야 할 규칙

1. **`GOOGLE_GEMINI_API_KEY`는 비워 둔다.** 작업 6을 하지 않으므로 파일럿 화면에는 필요 없다.
2. **`data/requests.json`과 `data/analytics.json`은 커밋하지 않는다.** 확인용 가상 데이터와 접속 통계가 들어 있어 변경 상태로 남아 있는 것이 정상이다.
3. **실데이터를 넣지 않는다.** 대상자·기관·연락처는 누가 봐도 가상인 값만 쓴다. 연계처 실연락처 입력은 공모 마감 뒤다.
4. **작업 1건이 끝날 때마다 즉시 푸시한다.** `git push origin pilot-sancheong`. 이전에 작업 2·4·5가 푸시되지 않은 채 유실된 적이 있다.
5. `master`는 건드리지 않는다.
6. `.env`, 실제 명단, API 키는 커밋하지 않는다.

### 로컬 실행

```
npm install
cp .env.example .env     # ADMIN_PASSWORD, WORKER_PASSWORD, ENCRYPTION_KEY 입력
node scripts/seed_cases.mjs   # 가상 대상자 4명
node server.js                # http://localhost:5000
```

`ENCRYPTION_KEY`는 64자 hex다. 생성: `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`
Gemini·Supabase·Resend 키가 없어도 파일럿 화면은 동작한다.
